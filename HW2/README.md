# HW2 — SQL

Выполни `homework2.sql` целиком в новой базе PostgreSQL. Скрипт создаёт таблицы, меняет их структуру, добавляет тестовые данные и выполняет три `UPDATE`.

Структура базы после выполнения скрипта: [скриншот](screenshots/structure.png).
